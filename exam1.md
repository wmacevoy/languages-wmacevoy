# Programming Lanbguages Spring 2025 Study Guide Exam 1

4 Models of Computation

4 Languages Paradigms

Why are saftey procecures written in procedural form?

In what way are microservices an example of functional programming?

Why is SQL a constraints language?

How do CSS selectors work - especially disambiguation?

Regular Expressions

What does (a|bat)?ing match?

Context Free Grammars



Scanning / Parsing

[Source Code]
Bytes -> Unicode Strings -> (Scanner) -> Tokens -> (Parser) -> Parse Tree -> Code Generator 

89 ax 97 33 22   --> uint8_t *data = { ... }
#include <iostream>[lf]int main() {....} --> std::string < uint32 >
[cpp directive: #include] [cpp string: <iostream>][type: int][id: 
main] ....

class Token { } --> DoubleLiteralToken, IfToken, OpToken, ...
Logic / Constraint Languages

CSS / SQL / HTML / JSON / Git

Functional Language

Node REST API (Uses JS which is general purpose in a functional way)

